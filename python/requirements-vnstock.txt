-r requirements.txt
vnstock>=4.0
# Optional institutional reference/event layer used by Corporate Actions.
# vnstock_data 3.x introduced the current Unified UI (Reference/Market/etc.).
# The core app still starts without it and reports the provider as UNAVAILABLE.
vnstock_data>=3.0.0
