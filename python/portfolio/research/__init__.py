"""QPort research package (PIT quant research foundation, T09-T13).

The research layer validates investment hypotheses ONLY. It does not generate
production BUY/SELL actions, does not mutate the ledger, and does not expose
expected alpha until an OOS-validated edge exists.
"""